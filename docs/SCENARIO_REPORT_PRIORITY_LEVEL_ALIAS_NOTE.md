# Scenario report priority-level alias note

Scenario files may now express report priority with `priority_level` or `priority_label` in addition to the existing `priority`, `urgency`, `importance`, and `report_priority` fields.

Use this when imported JSON/YAML fixtures come from a UI or export format that names the field more explicitly.
