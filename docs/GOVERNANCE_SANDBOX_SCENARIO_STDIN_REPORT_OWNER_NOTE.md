# GOVERNANCE_SANDBOX_SCENARIO_STDIN_REPORT_OWNER_NOTE

Keep the current phase-one lane narrow: one imported JSON or YAML scenario file (or stdin replay), one regenerated JSON/Markdown/HTML report bundle, and one owner-visible status line that names the source plus the output directory. This keeps scenario-file support and report generation ahead of trait presets or web-demo expansion.
