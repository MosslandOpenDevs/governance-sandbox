# Scenario session bundle note

Use `scenario_session_bundle` when a workshop or facilitation replay packages the proposal, stakeholder roster, and report metadata under one top-level wrapper.

Keep this alias in the same phase-one lane as JSON/YAML scenario-file support plus generated Markdown/HTML/JSON report bundles.
