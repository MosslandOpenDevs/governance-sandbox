# Scenario File Report Delivery Ladder Note

Keep the current delivery order explicit:

1. Scenario file input support (JSON / YAML)
2. Markdown / HTML report generation alongside JSON output
3. Stakeholder trait presets (`dao`, `delegates`, `contributors`, `investors`, `community`)
4. One small web demo: one form, one result card, one stable replay path
5. README demo GIF after the web flow is stable

Do not count web-demo polish as phase-one completion if the scenario import or report bundle proof still regressed.
