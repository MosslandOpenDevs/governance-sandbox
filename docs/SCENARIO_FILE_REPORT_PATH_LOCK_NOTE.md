# Scenario file report path lock note

Keep phase-one work anchored to one imported JSON/YAML scenario plus one regenerated markdown/html/json report bundle, and re-check the artifact paths before widening presets or web-demo scope.
