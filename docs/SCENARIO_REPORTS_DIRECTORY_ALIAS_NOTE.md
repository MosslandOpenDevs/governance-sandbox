# Scenario reports_directory alias note

Scenario files may use `reports_directory` when the report bundle directory should read naturally in reviewer-facing exports. The CLI treats it the same as the existing report-directory aliases and still regenerates the JSON, Markdown, and HTML bundle together.
