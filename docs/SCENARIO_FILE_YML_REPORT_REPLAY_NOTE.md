# SCENARIO_FILE_YML_REPORT_REPLAY_NOTE

Treat `.yml` scenario files the same as `.yaml` inputs: one scenario source in, one JSON/Markdown/HTML report bundle out, then validate before widening preset or web-demo scope.
