# governance-sandbox proposal-input-file alias note

Use `proposal_input_file` when a scenario fixture should load proposal markdown from disk before generating the same JSON, Markdown, and HTML report bundle.
