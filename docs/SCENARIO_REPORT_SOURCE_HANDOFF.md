# Scenario report source handoff

Keep scenario-file provenance visible in the generated report bundle. When a scenario comes from JSON, YAML, or stdin, preserve that source in the report metadata so reviewers can reopen the exact input path before widening the web-demo scope.
