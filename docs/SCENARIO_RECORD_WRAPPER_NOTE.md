# Scenario record wrapper note

Use `scenario_record` when a workshop export or saved replay bundle wraps the same proposal, stakeholder roster, and report metadata under one archive-style key.
Keep the fixture replayable: one imported `scenario_record` should still generate the same JSON, Markdown, and HTML report bundle as the equivalent top-level scenario file.
