# Web demo result-card status line

Keep the first web demo status update scoped to one believable slice:

1. name one scenario input path,
2. name one result card outcome,
3. name one report artifact or download check.

Good status lines stay stable, reproducible, and narrow enough for replay.
