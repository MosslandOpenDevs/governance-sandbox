# Governance sandbox report_output_handle alias note

If one scenario export already uses `report_output_handle`, keep that top-level field as the shared basename for the generated JSON, Markdown, and HTML report bundle instead of reshaping the scenario file first.
