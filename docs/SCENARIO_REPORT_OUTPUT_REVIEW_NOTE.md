# Scenario report output review note

Keep the first governance-sandbox review loop anchored to one imported JSON/YAML scenario file and one generated JSON/Markdown/HTML report bundle.

Quick check:

1. Re-run the same `--scenario-file` input.
2. Confirm the report basename stays stable.
3. Open the Markdown or HTML memo first.
4. Verify the matching JSON artifact still points to the same proposal and stakeholder set.

Do not widen scope to preset or web-demo work until this replayable scenario-to-report bundle still feels trustworthy.
