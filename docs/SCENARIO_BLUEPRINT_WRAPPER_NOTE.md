# Scenario blueprint wrapper note

`scenario_blueprint` can wrap the same proposal + stakeholder payload as other scenario-file wrappers, and should still produce a JSON/Markdown/HTML report bundle from one replayable fixture.
