# Scenario-file report validate bundle note

Phase one stays simple: replay one JSON or YAML scenario file, regenerate the markdown/html/json report bundle, verify the output paths and metadata, then widen preset or web-demo work only after that bundle passes local review.
