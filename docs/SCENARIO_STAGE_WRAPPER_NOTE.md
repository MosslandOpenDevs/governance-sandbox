# Scenario Stage Wrapper Note

`scenario_stage` and `scenario_stage_bundle` are accepted as scenario-file wrappers.

Use them when a single JSON/YAML file needs to carry:

- proposal input,
- stakeholder presets,
- and markdown/html/json report metadata

inside one staged governance rehearsal bundle.
