# Governance-sandbox report directory alias note

Scenario-file work stays phase one when one imported scenario can name the report bundle directory directly and still regenerate the JSON, Markdown, and HTML outputs from the same replay.
