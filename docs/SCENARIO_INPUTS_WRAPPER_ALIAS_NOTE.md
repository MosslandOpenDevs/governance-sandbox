# Scenario inputs wrapper alias note

Use `scenario_inputs` as a plural top-level wrapper when a scenario file carries proposal text, stakeholder presets, and report metadata together. This keeps scenario authoring compact while preserving the same JSON/YAML replay path used by the default scenario-input import flow.
