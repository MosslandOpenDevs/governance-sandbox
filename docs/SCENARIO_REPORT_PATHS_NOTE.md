# Scenario report paths note

Use this note when the first governance-sandbox proof should stop at **one imported scenario file and one generated report bundle**.

Check these paths together:
- the resolved scenario file path;
- the report directory path;
- the JSON, Markdown, and HTML artifact paths.

This keeps scenario-file support and report-generation proof visible in the same verification step.
