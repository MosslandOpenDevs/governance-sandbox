# Governance sandbox scenario source link alias note

Keep imported scenario metadata flexible while phase-one work stays scenario-file/report-first: accept `scenario_link` or `source_link` as the same reviewer-facing scenario source cue, then rerun the smallest report-bundle validation before widening the web demo.
