# Scenario file report status bridge note

Use this note when one governance-sandbox status update should stay anchored on the current phase-one pair.

Keep the bridge explicit:
- import one scenario file (JSON or YAML)
- regenerate one named JSON/Markdown/HTML report bundle
- keep validation green before commit/push
