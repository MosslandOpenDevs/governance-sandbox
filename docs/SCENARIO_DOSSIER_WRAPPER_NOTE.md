# Scenario Dossier Wrapper Note

`scenario_dossier` is accepted as a scenario-file wrapper when a proposal packet arrives as one dossier object instead of a flat scenario map.

Use it the same way as the other scenario wrappers: keep the proposal, stakeholders, and optional report metadata nested together so one JSON/YAML file can drive the full run.
