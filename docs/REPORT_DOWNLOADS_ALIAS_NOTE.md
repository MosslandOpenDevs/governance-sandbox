# report_downloads alias note

Scenario files can use `report.outputs.downloads` or top-level `report_downloads` when one imported rehearsal should regenerate download-oriented JSON, Markdown, and HTML report files without reshaping the fixture first.
