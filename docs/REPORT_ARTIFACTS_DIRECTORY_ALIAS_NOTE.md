# REPORT_ARTIFACTS_DIRECTORY_ALIAS_NOTE

Scenario files may now place the shared report bundle directory under `report.outputs.artifact_dir`, `report.outputs.artifacts_dir`, or `report.outputs.artifacts_directory`.

Use that alias when imported JSON/YAML rehearsal packs already describe report artifacts as an artifacts directory but still need the same markdown/html/json bundle generation path.
