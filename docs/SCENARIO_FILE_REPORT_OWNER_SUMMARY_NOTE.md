# Scenario-file report owner-summary note

Keep the phase-one governance-sandbox slice tied to:
- one JSON or YAML scenario file,
- one markdown/html/json report bundle,
- one owner-facing summary line for the generated artifacts.

That keeps scenario import proof and report-output proof coupled before moving on to presets or the web demo.
