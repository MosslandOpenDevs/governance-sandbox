# Scenario file + report phase-one checklist

Keep the current delivery order explicit:

1. Accept one JSON or YAML scenario file that imports proposal + stakeholders.
2. Generate one replayable report bundle with JSON, Markdown, and HTML outputs.
3. Expand stakeholder trait presets only after the scenario/report lane stays stable.
4. Hold the web demo to one form and one result card until the CLI report bundle is believable.
5. Add the demo GIF after the browser proof is stable and reproducible.

This note exists to keep phase-one work focused on scenario-file input plus Markdown/HTML report generation.
