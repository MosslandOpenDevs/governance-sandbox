# Governance sandbox report outputs label alias note

Use `report.outputs.label` when a scenario file needs one short, reviewer-facing basename alias for the generated JSON, Markdown, and HTML report bundle.

Keep it in the same phase-one lane: scenario file first, regenerated report bundle second, presets and web-demo follow-up later.
