# Scenario file report replay card

Keep the current priority lane narrow: one JSON or YAML scenario file should replay into one generated report bundle with predictable JSON, Markdown, and HTML outputs.

Before widening preset or web-demo work, confirm that the same imported scenario still reproduces the same report bundle paths and report headings.
