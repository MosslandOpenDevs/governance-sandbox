# Scenario report takeaway alias note

Scenario files can use `report_takeaway` or `takeaway` as a short reviewer-facing summary alias when the same one-line memo should flow into generated markdown and HTML reports. This keeps imported JSON/YAML scenario packs readable without forcing a rename to `report_summary`.
