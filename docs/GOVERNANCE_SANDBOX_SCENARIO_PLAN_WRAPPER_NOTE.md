# Governance Sandbox scenario_plan wrapper note

Use `scenario_plan` when a JSON/YAML fixture needs one obvious wrapper for the imported proposal, stakeholder roster, and report metadata. Keep the same replay lane: one scenario file in, one JSON/Markdown/HTML report bundle out.
