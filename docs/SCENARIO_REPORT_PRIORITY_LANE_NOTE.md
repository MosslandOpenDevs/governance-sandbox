# Scenario report priority lane note

Scenario files may describe reviewer urgency with `priority_lane` or `urgency_label` when the source payload already uses workflow-lane wording instead of plain `priority`.

Keep those aliases tied to the same generated markdown/html report priority section so fixture imports, browser demos, and saved reports stay consistent.
