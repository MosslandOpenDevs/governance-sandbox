# Scenario casefile wrapper note

Use `scenario_casefile` or `scenario_casefile_bundle` when a JSON/YAML scenario import arrives inside a casefile-style wrapper and still needs to feed the same JSON/Markdown/HTML report bundle proof lane before preset or demo work.
