# Governance sandbox scenario source path alias note

When a scenario file is routed through wrappers or stdin replays, keep provenance with either alias below:

- `scenario_path`
- `source_path`

Saved markdown and HTML reports should surface that path so reviewers can reopen the same scenario input without guessing.
