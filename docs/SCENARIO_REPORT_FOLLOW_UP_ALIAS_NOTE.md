# Scenario report follow-up alias note

When a scenario file already stores the reviewer handoff sentence as a follow-up note, `report_follow_up` can feed the generated report summary without duplicating the same text under another key.

Use this for small scenario bundles that want one phase-one report line to survive into JSON, Markdown, and HTML outputs together.
