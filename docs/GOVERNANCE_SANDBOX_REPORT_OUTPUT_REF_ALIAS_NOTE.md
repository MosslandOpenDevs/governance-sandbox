# governance-sandbox report output ref alias note

If one scenario export already uses `report_output_ref`, the same JSON/Markdown/HTML report bundle can keep that top-level reference as the shared basename without reshaping the file first.
