# Governance Sandbox report reviewers alias note

Use `report.reviewers` when a scenario author wants reviewer-facing audience labels without changing the generated markdown/html/json report lane.

Keep it equivalent to the existing audience/readers/viewers aliases so one scenario file can still drive the same report bundle and reviewer handoff.
