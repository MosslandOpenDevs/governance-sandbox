# scenario review-pack wrapper note

Scenario files wrapped under `scenario_review_pack` or `scenario_review_pack_bundle` should resolve through the same import path as direct scenario payloads so one JSON/YAML file can still generate the report bundle without reshaping.
