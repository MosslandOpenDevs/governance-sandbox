# Scenario report short flag note

The scenario-file -> report-bundle path now accepts compact CLI spellings for the two review surfaces most often replayed first:

- `--report-md` for the Markdown memo
- `--report-htm` for the HTML report

Use them when you want a shorter replay command without changing the scenario-first, report-bundle-first delivery order.
