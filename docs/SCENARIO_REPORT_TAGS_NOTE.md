# Scenario Report Tags Note

Keep scenario tags visible from the source fixture through the generated report so reviewers can tell which rehearsal theme or workstream the report belongs to without reopening raw JSON or YAML.
