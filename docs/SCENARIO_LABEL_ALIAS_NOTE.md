# Scenario label alias note

Scenario files may name the scenario with `scenario_label` or `scenario_heading` when `name`, `title`, or `scenario_title` are not present.

This keeps imported JSON/YAML scenario packets compatible with the same markdown/html/json report metadata lane.
