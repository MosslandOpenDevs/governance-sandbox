# Governance sandbox proposal_body alias note

Scenario files may place the main proposal text under `proposal_body` when workshop or form exports use that field name.

The same imported scenario should still drive stakeholder simulation plus JSON/Markdown/HTML report generation without needing a fixture rewrite.
