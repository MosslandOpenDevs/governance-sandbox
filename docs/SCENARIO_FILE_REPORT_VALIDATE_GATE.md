# Scenario file report validate gate

Phase one stays scenario-file first and report-bundle second: replay one real JSON or YAML scenario input, regenerate the JSON/Markdown/HTML bundle, and only treat the slice as done after that smallest report proof passes.
