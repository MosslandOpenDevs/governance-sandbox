# Governance Sandbox report output label alias note

Use `report.output_label` or `report.outputs.output_label` when a scenario file needs one reviewer-facing bundle label that still resolves into the generated JSON, Markdown, and HTML artifact basename. Keep it in the same phase-one lane: imported scenario file first, regenerated report bundle second, broader preset/demo work later.
