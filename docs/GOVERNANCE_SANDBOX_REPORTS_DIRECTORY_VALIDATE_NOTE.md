# Governance Sandbox reports_directory validate note

Before widening presets or web demo scope, keep the phase-one proof to one imported JSON/YAML scenario file, one regenerated JSON/Markdown/HTML bundle under one `reports_directory`, and one fresh validation rerun.
