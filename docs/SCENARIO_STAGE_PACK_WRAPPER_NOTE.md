# Scenario Stage Pack Wrapper Note

Use `scenario_stage_pack` or `scenario_stage_pack_bundle` when a governance scenario export wants stage-oriented naming without changing the underlying scenario-file contract. The same wrapper still feeds proposal import, stakeholder loading, and JSON/Markdown/HTML report generation.
