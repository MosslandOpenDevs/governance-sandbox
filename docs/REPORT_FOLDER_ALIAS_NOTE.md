# report_folder alias note

Use `report_folder` when you want a short top-level scenario field that points to the generated JSON/Markdown/HTML report bundle directory.
Keep it scoped to one scenario replay so the report location stays obvious in review and demo handoff.
