# scenario preset report start

Start the governance-sandbox growth path with one scenario file that already names preset-backed stakeholders and one report directory.

Minimum proof:

- load proposal + stakeholders from JSON or YAML,
- resolve stakeholder presets from the scenario file,
- emit the JSON result,
- emit a markdown report,
- emit an HTML report.

Keep this slice stable before widening into richer UI flows.
