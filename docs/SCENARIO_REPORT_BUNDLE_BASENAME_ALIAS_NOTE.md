# Scenario report bundle basename alias

If you want scenario files to name the generated JSON/Markdown/HTML bundle with a slightly more explicit field, use `report.outputs.bundle_basename` as the same contract as `report.outputs.basename`.
