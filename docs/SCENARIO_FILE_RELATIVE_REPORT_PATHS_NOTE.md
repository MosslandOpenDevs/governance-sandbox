# Scenario file relative report paths note

When a scenario file declares relative JSON, Markdown, or HTML report paths, resolve them from the scenario file directory so one fixture can carry its own reusable report bundle layout.
