# Governance Sandbox proposal input markdown-file alias note

- `proposal_input_markdown_file` and `proposal_input_md_file` let one JSON/YAML scenario load proposal body text from a neighboring markdown file.
- The same phase-one path still ends in regenerated JSON, Markdown, and HTML report artifacts.
- Keep this alias in the scenario-file -> report-bundle lane before wider preset or web-demo work.
