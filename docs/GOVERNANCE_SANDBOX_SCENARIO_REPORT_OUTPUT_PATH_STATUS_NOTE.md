# Governance sandbox scenario report output path status note

Use this once one scenario file already generated the review bundle and you need a short replay status line before widening scope.

Keep the line anchored to:
- scenario input path
- report basename
- generated markdown / html / json output paths

Example:

`Replay ready: examples/scenario-inputs-report-outputs.yaml produced the stakeholder-review bundle and wrote markdown, html, and json outputs under reports/demo/.`
