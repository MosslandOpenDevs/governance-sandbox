# Scenario trait preset phase queue note

Use this note after the scenario-file and report-bundle path already feels stable.

## Queue order

1. Keep the first real win on JSON/YAML scenario import plus generated JSON/Markdown/HTML report outputs.
2. Add stakeholder trait presets only after the same scenario file still replays cleanly.
3. Start with the core preset pack: DAO, delegates, contributors, investors, community.
4. Defer broader web-demo or GIF polish until the preset-backed scenario flow is still easy to replay.

## Validation cue

Tie any preset expansion to the smallest scenario/report test rerun before commit/push.
