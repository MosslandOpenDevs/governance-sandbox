# SCENARIO_FILE_REPORT_MARKDOWN_OUTPUT_ALIAS_NOTE

When a scenario file should emit a human-readable memo without reopening nested report blocks, allow the top-level `report_markdown_output` alias to point directly at the Markdown report path.
