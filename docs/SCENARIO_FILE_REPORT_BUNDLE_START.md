# Scenario file report bundle start

Start with one JSON or YAML scenario file, generate the markdown/html/json report bundle in one pass, and review the produced artifact paths before adding broader stakeholder preset or web demo work.
