# Scenario report target alias note

Top-level `report_markdown_target` and `report_html_target` aliases resolve through the same scenario-file -> report bundle flow as the existing markdown/html output aliases.
