# SCENARIO_FILE_REPORT_JSON_YAML_PRIORITY_NOTE

Keep the current phase-one order explicit:

1. load one scenario file from JSON or YAML;
2. regenerate one report bundle in JSON / Markdown / HTML;
3. validate that smallest replay before widening presets or web-demo scope.

This note is for maintainer handoffs when the repo needs a one-screen reminder that scenario-file input plus report generation is still the minimum believable governance slice.
