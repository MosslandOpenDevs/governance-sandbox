# SCENARIO_REPORT_WATCHERS_ALIAS_NOTE

Use `report.watchers` or `report_watchers` when the memo audience is easiest to phrase as watchers.
Treat it as an audience alias only: keep the same imported scenario and generated JSON/Markdown/HTML report bundle.
