# governance-sandbox report bundle code alias note

Keep phase-one progress anchored to scenario-file input plus report output proof.

- `report_bundle_code` is accepted as a top-level alias for the generated report bundle basename.
- Use it when a scenario author wants one short, reviewer-friendly bundle name for the JSON/Markdown/HTML trio.
- Re-run the smallest scenario-file replay after changing bundle-name aliases so scenario-file input and report output stay coupled.
