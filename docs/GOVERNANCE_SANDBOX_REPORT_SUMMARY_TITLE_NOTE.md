# GOVERNANCE_SANDBOX_REPORT_SUMMARY_TITLE_NOTE

Use `report.summary_title` when one imported scenario should stamp the same human-readable memo title into the generated Markdown and HTML report bundle while JSON output stays machine-friendly.

Keep it in the current phase-one lane: scenario file first, report bundle second, trait presets and web demo later.
