# Scenario reports_directory status note

Use this note when one imported JSON/YAML scenario regenerates its JSON, Markdown, and HTML outputs through the `reports_directory` alias and you need a short maintainer status line.

- Keep scenario-file input first.
- Keep report-bundle regeneration visible.
- Keep commit/push claims behind a passing replay.
