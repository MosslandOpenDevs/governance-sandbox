# scenario package report stack note

Use `scenario_package` when a governance-sandbox input bundle should carry proposal, stakeholders, and report-output metadata together. Keep the first proof loop scoped to one imported package plus one generated JSON/Markdown/HTML report stack.
