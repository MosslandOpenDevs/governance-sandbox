# Governance Sandbox scenario/report phase-one recheck

Keep phase-one work anchored to scenario files and generated reports.

- Start with one imported JSON or YAML scenario file.
- Rebuild the JSON, Markdown, and HTML report bundle from that same scenario.
- Keep presets, web demo, and GIF work behind that scenario/report proof.
- Re-run the smallest replayable validation path before commit or push.
