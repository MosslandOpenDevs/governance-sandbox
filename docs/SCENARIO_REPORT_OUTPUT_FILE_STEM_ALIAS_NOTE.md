# SCENARIO_REPORT_OUTPUT_FILE_STEM_ALIAS_NOTE

Scenario files may use `report.output_file_stem` or top-level `report_output_file_stem` when one imported JSON/YAML rehearsal should regenerate a shared markdown/html/json report bundle under one basename.
Keep this alias in the same phase-one lane as scenario-file input plus generated report artifacts.
