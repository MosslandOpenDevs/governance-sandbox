# Scenario file report-dir handoff note

Keep the current phase-one slice narrow: replay one JSON or YAML scenario file, generate one named report directory, and verify the shared JSON/Markdown/HTML bundle before widening stakeholder presets or web-demo scope. The handoff should still name the scenario source and the report directory together so the replay stays easy to reproduce.
