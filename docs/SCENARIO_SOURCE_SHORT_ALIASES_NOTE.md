# Scenario source short aliases

Use `scenario_src` or `source_href` when imported JSON/YAML scenario bundles need a shorter way to preserve the original scenario source path or URL inside the generated JSON/Markdown/HTML report bundle.
