# Scenario report phase-one replay lane

Phase one stays intentionally narrow:

1. import one JSON or YAML scenario file,
2. resolve proposal + stakeholder data,
3. regenerate one JSON, Markdown, and HTML report bundle,
4. rerun tests before push.

Use this note when maintainers need a compact reminder that scenario-file support and report generation remain the current top priority.
