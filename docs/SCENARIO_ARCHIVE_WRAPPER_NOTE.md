# Scenario archive wrapper note

Use `scenario_archive` when a saved governance rehearsal or workshop export keeps proposal input, stakeholder presets, and report metadata under one archive-style wrapper without reshaping the file first.
