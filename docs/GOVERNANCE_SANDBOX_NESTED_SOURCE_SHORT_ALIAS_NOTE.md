# GOVERNANCE_SANDBOX_NESTED_SOURCE_SHORT_ALIAS_NOTE

Nested `inputs` and `scenario_inputs` wrappers now accept `scenario_src` and `source_href` so imported JSON/YAML scenario files keep one visible scenario source tied to the generated JSON/Markdown/HTML report bundle.
