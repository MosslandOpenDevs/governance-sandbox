# Governance sandbox web demo download signoff

Use this note when the first governance-sandbox web demo already proves scenario import and one reviewer-visible result card.

- Keep the demo lane scoped to one scenario source, one result card, and one report-download action.
- Verify the downloaded JSON/Markdown/HTML bundle names match the visible card metadata.
- Keep browser replay and recovery notes next to the signoff step before expanding the UI.
