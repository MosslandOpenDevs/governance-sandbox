# governance-sandbox proposal input markdown path alias note

- `proposal_input_markdown_path` and `proposal_input_md_path` let one JSON/YAML scenario load proposal body text from a neighboring markdown file with explicit path-style naming.
- Keep the same phase-one proof lane: one imported scenario file, one regenerated JSON/Markdown/HTML report bundle, one passing validator before push.
