# Scenario Archive Report Stack Note

Treat `scenario_archive` as another thin scenario file wrapper, not a new workflow. Import the archived proposal + stakeholder payload, then prove the same JSON/Markdown/HTML report stack in one replayable pass before widening presets or web demo scope.
