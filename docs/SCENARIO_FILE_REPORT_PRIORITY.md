# Scenario-file and report priority

The current build order stays explicit:

1. scenario-file input support (JSON/YAML proposal + stakeholders)
2. markdown/html report generation beyond JSON output
3. stakeholder trait presets
4. simple web demo (input form + result card)
5. README demo GIF

Keep roadmap, README, and demo planning aligned to this order so scenario replay and report artifacts stay ahead of broader UI polish.
