# SCENARIO_STAKEHOLDER_TYPE_ALIAS_NOTE

Scenario files may describe stakeholder presets with `type` or `stakeholder_type` when the payload already reserves `role` for a human-readable label. The alias should still resolve to the same DAO/delegates/contributors/investors/community trait presets and feed the generated markdown/html/json report bundle.
