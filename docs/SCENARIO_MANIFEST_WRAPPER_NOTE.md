# Scenario manifest wrapper note

Scenario files may now wrap the runnable payload under `scenario_plan` or `scenario_manifest` when proposal, stakeholders, and report metadata need one outer envelope for handoff.
