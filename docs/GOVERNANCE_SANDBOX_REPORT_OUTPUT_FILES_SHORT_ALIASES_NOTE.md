# Governance sandbox report output files short aliases

When a scenario file already uses `report.outputs.files`, the shortest explicit bundle keys can be:

- `json_report`
- `markdown_report`
- `html_report`

Use them when you want JSON, Markdown, and HTML report paths to stay obvious in one scenario fixture without mixing top-level aliases into the same file.
