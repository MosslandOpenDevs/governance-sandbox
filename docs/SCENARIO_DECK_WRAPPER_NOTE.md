# Scenario deck wrapper note

If a proposal fixture arrives under `scenario_deck` or `scenario_deck_bundle`, the same JSON/YAML scenario-file import path should still resolve proposal, stakeholders, and report metadata without reshaping the file first.
