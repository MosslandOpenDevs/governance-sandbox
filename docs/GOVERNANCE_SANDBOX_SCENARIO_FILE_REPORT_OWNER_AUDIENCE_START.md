# Governance Sandbox Scenario-File Report Owner + Audience Start

Start with one JSON/YAML scenario file that imports the proposal and stakeholder roster, then prove the first generated markdown/html/json bundle keeps both `report.owner` and `report.audience` visible before widening preset or web-demo scope.
