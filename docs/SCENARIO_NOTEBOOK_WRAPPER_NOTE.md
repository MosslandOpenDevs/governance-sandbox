# SCENARIO_NOTEBOOK_WRAPPER_NOTE

Scenario files wrapped as `scenario_notebook` or `scenario_notebook_bundle` resolve through the same JSON/YAML import path, so notebook-style workshop exports can still generate the same JSON/Markdown/HTML report bundle without reshaping the file first.
