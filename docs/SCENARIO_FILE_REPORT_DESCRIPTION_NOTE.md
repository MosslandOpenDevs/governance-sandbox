# Scenario-file report description note

Keep one imported JSON or YAML scenario tied to one generated Markdown/HTML/JSON report bundle that exposes the memo description directly in human-readable outputs.

Use this when you want the scenario-file lane to stay coupled to report readability work before widening stakeholder presets or web-demo scope.
