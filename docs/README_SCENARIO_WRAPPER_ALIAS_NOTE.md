# Scenario wrapper alias note

Use this when a JSON or YAML fixture adds one extra envelope such as `scenario_payload`, `scenario_data`, `scenario_bundle`, or `scenario_spec`. The CLI should unwrap the inner proposal, stakeholder list, and report metadata before generating JSON, Markdown, and HTML outputs.
