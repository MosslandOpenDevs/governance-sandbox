# Scenario-file markdown/html replay note

Keep the current phase-one slice simple and reproducible:

1. load one JSON or YAML scenario file
2. regenerate the JSON report artifact
3. regenerate the Markdown memo
4. regenerate the HTML memo from the same scenario input

Use one report directory or shared basename so reviewers can reopen the full bundle without guessing file names.
