# Scenario report priority ladder note

Keep governance-sandbox delivery in this order:

1. import one JSON/YAML scenario file,
2. generate one JSON/Markdown/HTML report bundle,
3. widen preset coverage,
4. shape the first web demo,
5. capture the demo GIF after the UI slice is stable.

For web UI work, keep one scenario form, one result card, and one report-download proof in the same replayable slice.
