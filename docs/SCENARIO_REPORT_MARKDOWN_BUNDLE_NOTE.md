# Scenario report markdown bundle note

Keep the first scenario-report handoff easy to reopen.

- Start from one imported scenario file.
- Generate one markdown report plus the matching JSON payload.
- Reuse one shared basename so the human-readable report and machine-readable artifact stay paired.
