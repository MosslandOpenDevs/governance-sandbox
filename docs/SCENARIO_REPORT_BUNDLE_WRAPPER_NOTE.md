# Scenario report bundle wrapper note

Use `scenario_report_bundle` or `scenario_report_bundle_pack` when one imported JSON/YAML rehearsal already groups scenario input with report-focused handoff metadata.

The CLI should still load the nested proposal and stakeholder data, then regenerate the same JSON/Markdown/HTML report bundle without reshaping the source file first.
