# Scenario proposal copy file alias note

Use `proposal_copy_file` when a scenario JSON/YAML bundle should load the proposal body from a nearby markdown file while keeping the same scenario-file-first report generation flow.
