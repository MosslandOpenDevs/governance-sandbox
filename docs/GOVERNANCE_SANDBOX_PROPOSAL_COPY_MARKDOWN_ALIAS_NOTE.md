# proposal_copy_markdown alias note

Use `proposal_copy_markdown` in a scenario file when you want proposal body markdown to flow through the same JSON/Markdown/HTML report lane as the existing proposal body aliases. Keep this alias in the scenario-file-first slice before widening web-demo work.
