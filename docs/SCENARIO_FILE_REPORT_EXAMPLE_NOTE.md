# Scenario file report example note

Use `examples/dao-growth-report-bundle.json` when you want one compact proof that covers both current priorities:

- scenario-file proposal + stakeholder import, and
- generated markdown / html / json report artifacts under one basename.

This keeps the scenario-input lane and the report-output lane visible in a single runnable example.
