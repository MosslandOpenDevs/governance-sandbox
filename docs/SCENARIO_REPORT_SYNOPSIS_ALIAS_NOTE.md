# Scenario report synopsis alias note

Use `report_synopsis` when a scenario file already has memo-summary wording that should flow into the generated JSON/Markdown/HTML report bundle without renaming the field first.
