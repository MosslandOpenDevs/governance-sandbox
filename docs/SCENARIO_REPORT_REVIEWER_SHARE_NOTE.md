# Scenario Report Reviewer Share Note

Use this note when one scenario file already generates the full JSON/Markdown/HTML bundle and you need a reviewer-ready reminder.

- Keep the imported scenario file path visible.
- Keep the generated markdown/html/json outputs visible together.
- Share the memo only after the same replay still passes from the scenario file.
