# Scenario source_file alias note

Scenario bundles may keep the replay pointer under `scenario_source_file` when teams want the file-first wording to stay explicit. Treat it the same as `source_file`/`source_path`: one imported JSON or YAML scenario should still regenerate the same JSON, Markdown, and HTML report bundle.
