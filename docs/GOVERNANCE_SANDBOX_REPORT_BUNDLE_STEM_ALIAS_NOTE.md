# Governance sandbox report bundle stem alias note

Scenario files can also use `report_bundle_stem` as a top-level phase-one basename alias when one imported JSON or YAML rehearsal should still regenerate the same JSON, Markdown, and HTML report bundle before preset expansion or web-demo polish.
