# Scenario dossier report bundle note

`scenario_dossier` and `scenario_dossier_bundle` are accepted as scenario-file wrappers, so one proposal packet can still drive the same JSON, Markdown, and HTML report bundle without reshaping the file first.
