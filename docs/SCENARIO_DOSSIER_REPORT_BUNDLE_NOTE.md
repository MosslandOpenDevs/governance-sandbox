# Scenario dossier report bundle note

`scenario_dossier` is accepted as a scenario-file wrapper, so one proposal packet can still drive the same JSON, Markdown, and HTML report bundle without reshaping the file first.
