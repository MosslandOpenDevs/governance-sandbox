# Governance Sandbox Report Digest Alias Note

`report.digest` can act as a short summary alias for generated JSON, Markdown, and HTML governance report bundles.

Use it when scenario authors want one compact memo sentence without switching to a longer `description` or `executive_summary` field.
