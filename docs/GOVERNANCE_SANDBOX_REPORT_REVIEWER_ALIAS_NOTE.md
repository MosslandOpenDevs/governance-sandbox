# Governance Sandbox report reviewer alias note

Use `report.reviewer` or top-level `report_reviewer` when one primary memo reader should drive the rendered `Report audience` field without switching to a plural-only fixture shape.
