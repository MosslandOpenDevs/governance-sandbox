# Scenario journal wrapper note

Use `scenario_journal` or `scenario_journal_bundle` when one exported governance rehearsal already groups the proposal, stakeholder roster, and report bundle metadata under a journal-shaped wrapper.

Keep the phase-one proof small: load one JSON/YAML scenario file, regenerate the JSON/Markdown/HTML report bundle, and verify the same scenario-source trail still renders in the artifacts.
