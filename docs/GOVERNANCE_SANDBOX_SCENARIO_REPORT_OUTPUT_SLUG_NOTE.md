# Governance sandbox scenario report output slug note

Use this during the current phase-one build order when one imported scenario file should replay into the same visible output slug for JSON, Markdown, and HTML artifacts.

Keep the proof tight:
- start from one real scenario file
- keep the JSON/Markdown/HTML bundle aligned under one output slug
- validate the smallest replayable slice before widening presets or web-demo scope
