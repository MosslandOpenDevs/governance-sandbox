# Scenario Rehearsal Wrapper Note

`scenario_rehearsal` is accepted as a top-level wrapper when a fixture wants to group proposal input, stakeholder presets, and report metadata under one rehearsal-oriented key.

Use it when exported workshop or demo files already label the bundle as a rehearsal, but still keep the same scenario-file -> markdown/html/json report proof lane.
