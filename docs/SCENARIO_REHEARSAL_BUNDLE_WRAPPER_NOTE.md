# scenario rehearsal bundle wrapper note

Use `scenario_rehearsal_bundle` when a saved workshop replay keeps proposal text, stakeholder inputs, and report metadata under one rehearsal-labeled archive key.

Keep the fixture replayable: one imported `scenario_rehearsal_bundle` should still generate the same JSON, Markdown, and HTML report bundle as the equivalent top-level scenario file.
