# Scenario file report index card note

Keep the first validated governance-sandbox slice small and replayable: one JSON or YAML scenario file in, one JSON/Markdown/HTML report bundle out, then report the artifact paths in a short maintainer status line.
