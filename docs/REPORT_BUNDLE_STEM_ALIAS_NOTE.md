# Report bundle stem alias note

Use `report.bundle_stem` or `report.outputs.bundle_stem` when a scenario fixture wants a shorter bundle-naming key while still driving the same generated JSON / Markdown / HTML artifact basename.
