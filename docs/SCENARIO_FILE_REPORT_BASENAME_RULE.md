# Scenario file report basename rule

When a scenario file generates JSON, Markdown, and HTML reports together, keep one explicit basename in the fixture or report metadata.
That makes CLI replays, artifact review, and future web-demo download checks point at the same report bundle.
