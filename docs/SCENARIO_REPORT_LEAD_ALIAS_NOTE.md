# Scenario report lead alias note

Scenario files may use `report_lead` or `report_leads` when the export source names the report owner as a lead instead of an owner/author.

The alias resolves into the same report owner field used by JSON, Markdown, and HTML report generation.
